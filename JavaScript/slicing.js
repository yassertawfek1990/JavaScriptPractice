var total_characters = 180;
var message = prompt("type your message here")
var sliced_message = message.slice(0,total_characters)
alert("you have typed " + message.length + " characters, your edite message is: " + sliced_message )